import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
  project: "proj_qnvzrrduyvfunicemipq",
  runtime: "node",
  logLevel: "log",
  maxDuration: 600,
  dirs: ["./trigger"],
});
